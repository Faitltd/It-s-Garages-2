# Commit Signing

- Enable Vigilant Mode or require signed commits in repo settings.
- Use GPG or SSH signatures per GitHub docs.
- CI will fail if unsigned commits are pushed when protection is enabled.

